.container
  .follow
    %h2 Followers
  - @followers.each do |follower|
    = link_to account_path(follower.id) do
      .follow_detail
        = image_tag follower.avatar_image
        .follow_name
          = follower.account
